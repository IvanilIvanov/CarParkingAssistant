@using CarParkingAssistant.Infrastructure.Data.Models
@using Microsoft.AspNetCore.Identity
@inject SignInManager<CarParkingAssistantUser> SignInManager
@inject UserManager<CarParkingAssistantUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    <a class="site-user-login" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">@User.Identity?.Name</a>

@*     <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
        <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
    </form>
 *@}
else
{
    <a class="site-user-login" asp-area="Identity" asp-page="/Account/Login">Login</a>
}


@* <a href="/login" class="site-user-login">Login/Register</a> *@
@* <a href="@Url.Action("Index", "Booking")" class="site-nav-hyperlink hyperlink-undecorated">Book</a> *@
